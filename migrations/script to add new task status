set role poweruat;
insert into task_status(organisation_id,
                        uuid,
                        is_voided,
                        version,
                        created_by_id,
                        last_modified_by_id,
                        created_date_time,
                        last_modified_date_time,
                        name,
                        task_type_id,
                        is_terminal)
VALUES (240,
        uuid_generate_v1(),
        false,
        0,
        (select id from users where username = 'nupoork@poweruat'),
        (select id from users where username = 'nupoork@poweruat'),
        current_timestamp,
        current_timestamp,
        'Call again',
        1,
        true);
